// import { User } from './user';

export interface Service{
    id: string
    title:string,
    qualification: number,
    price: number,
    description: string,
    category: string,
    imgUrl?: string;
}